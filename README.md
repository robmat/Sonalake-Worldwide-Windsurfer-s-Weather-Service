# Sonalake-Worldwide-Windsurfer-s-Weather-Service
Sample app, spring showcase.

## Buidling and Running the Application
### Prerequisites
- Java 25
- docker (for macos use: `sudo ln -s $HOME/.docker/run/docker.sock /var/run/docker.sock`)
- docker-compose
